import Link from 'next/link';import { prisma } from '@/lib/db';import { StickyControls } from '@/components/shared/sticky-controls';
export default async function Page(){const tools=await prisma.toolItem.findMany({where:{published:true}});return <div className='space-y-4'><h1 className='text-3xl font-bold'>Tools</h1><StickyControls/><div className='grid gap-3 md:grid-cols-2'>{tools.map(t=><Link key={t.id} href={`/tools/${t.slug}`} className='glass p-4'><h3 className='font-semibold'>{t.title}</h3><p className='text-zinc-300'>{t.clickbaitSubtitle}</p><span className='btn-orange mt-3 inline-block'>Open tool</span></Link>)}</div></div>}
